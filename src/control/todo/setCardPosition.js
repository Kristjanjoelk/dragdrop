import actions from '../../actions';

const setCardPosition = (store, newPos, card) => {
    // console.log('Setting new card position for card', card);
    return;
};


export default {
    setCardPosition,
};

